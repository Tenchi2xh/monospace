
def block_fits(ast_block, page_settings):
    pass


def draw_page(ast, page_settings=None):
    # - figure out how much is needed by settings (headers, footers, margins)
    # - draw as many blocks as possible
    # - if block doesn't fit in page and will not fit entirely in next page,
    #   split block
    # - return new ast with remaining data/block leftovers
    pass
